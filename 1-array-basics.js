
//an array is a type of variable,
//arrays can contain strings, integers, bools, objects and even other arrays.
let scores = [92, 79, 64, 97, 58, 83, 49, 73, 90];


console.log(scores[1])



// we can find the size of the array by using the .length method
// console.log(scores.length);

// we can access individual members of an array
// by calling the arrays name (scores) followed 
// by [] with index inside of the square brackets
// **NOTE the first member of the array will be index 0**

const firstScore = scores[0];
const secondScore = scores[1];
const thirdScore = scores[2]

// scores[0] = scores[0] + 3
// console.log(scores)



// console.log(scores[3])